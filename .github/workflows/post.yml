name: Автопостинг в канал

on:
  schedule:
    # Время указано в UTC. МСК = UTC+3, без перевода часов.
    - cron: "0 5 * * *"   # 08:00 МСК — утро, лента после кофе
    - cron: "0 10 * * *"  # 13:00 МСК — обеденный перерыв
    - cron: "0 16 * * *"  # 19:00 МСК — вечер, дорога домой
  workflow_dispatch: {}  # позволяет запустить вручную кнопкой на GitHub

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Установка Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Установка зависимостей
        run: pip install -r requirements.txt

      - name: Запуск бота
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHANNEL_ID: ${{ secrets.TELEGRAM_CHANNEL_ID }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: python bot.py

      - name: Сохранить базу постов обратно в репозиторий
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add posted.db
          git diff --staged --quiet || git commit -m "update posted.db [skip ci]"
          git pull --rebase origin main
          git push
